#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../js-yaml/bin/js-yaml.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../js-yaml/bin/js-yaml.js" "$@"
  ret=$?
fi
exit $ret
